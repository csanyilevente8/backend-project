# Backend CI (currently DISABLED)
#
# This workflow is intentionally commented out so that pushing to GitHub does
# NOT trigger any build for now. To enable CI later, uncomment the block below.
# It is configured to run on the `main` branch and on pull requests.
#
# name: Backend CI
#
# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]
#
# jobs:
#   build:
#     name: Test and package
#     runs-on: ubuntu-latest
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Set up JDK 17
#         uses: actions/setup-java@v4
#         with:
#           distribution: temurin
#           java-version: '17'
#           cache: maven
#
#       # GitHub-hosted Ubuntu runners include Docker, which Testcontainers uses
#       # to start a real PostgreSQL instance for the integration tests.
#       - name: Run tests
#         run: mvn -B test
#
#       - name: Package
#         run: mvn -B package -DskipTests
#
#       - name: Upload jar
#         uses: actions/upload-artifact@v4
#         with:
#           name: todo-backend
#           path: target/*.jar
#           if-no-files-found: error
